(()=>{'use strict';
const DB_KEY='designlineERP_v2';
const CLOUD_URL_KEY='designlineERP_cloud_url';
const VIEW_KEY='designlineERP_active_view_v11';
const MSG_KEY='designlineERP_v11_msg';
const VIEWS=['dashboard','orders','quotations','customers','expenses','vendors','reports','settings'];
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const N=x=>Number(x)||0,R=x=>'Rs '+N(x).toLocaleString('en-PK'),E=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const read=()=>{try{return JSON.parse(localStorage.getItem(DB_KEY))||{}}catch(e){return{}}};
const write=db=>localStorage.setItem(DB_KEY,JSON.stringify(db));
function validView(v){return VIEWS.includes(String(v||''))}
function remember(v){if(validView(v))sessionStorage.setItem(VIEW_KEY,v)}
function activeView(){const el=$('.view.active');if(!el)return'';return String(el.id||'').replace(/View$/,'')}
function restore(){const v=sessionStorage.getItem(VIEW_KEY);if(!validView(v))return;const target=$('#'+v+'View');if(!target)return;$$('.view').forEach(x=>x.classList.remove('active'));target.classList.add('active');$$('.nav-btn[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===v));const nav=$('.nav-btn[data-view="'+v+'"]'),title=$('#pageTitle');if(title&&nav)title.textContent=nav.textContent.trim()}
function toast(t){const e=$('#toast');if(!e)return;e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1900)}
function cloudUrl(db){return String(db?.settings?.apiUrl||localStorage.getItem(CLOUD_URL_KEY)||'').trim()}
function immediateCloudSync(){try{const db=read(),u=cloudUrl(db);if(!u)return;const body=JSON.stringify({action:'syncAll',data:db});if(navigator.sendBeacon){const ok=navigator.sendBeacon(u,new Blob([body],{type:'text/plain;charset=UTF-8'}));if(ok)return}fetch(u,{method:'POST',mode:'no-cors',keepalive:true,headers:{'Content-Type':'text/plain;charset=utf-8'},body}).catch(()=>{})}catch(e){}}
function installSaveGuard(){if(window.__DL_V11_SAVE_GUARD__)return;window.__DL_V11_SAVE_GUARD__=true;const prev=Storage.prototype.setItem;Storage.prototype.setItem=function(k,v){const out=prev.call(this,k,v);if(this===localStorage&&k===DB_KEY)immediateCloudSync();return out}}
function vendorUsage(db,id){let jobs=0;(db.orders||[]).forEach(o=>{if(Array.isArray(o.lineItems)&&o.lineItems.length){o.lineItems.forEach(l=>{if(l.vendorId===id)jobs++})}else if(o.vendorId===id)jobs++});const pays=(db.vendorPayments||[]).filter(p=>p.vendorId===id).length;return{jobs,pays}}
function unlinkVendorFromOrders(db,id){(db.orders||[]).forEach(o=>{if(Array.isArray(o.lineItems)&&o.lineItems.length){o.lineItems.forEach(l=>{if(l.vendorId===id){l.vendorId='';delete l.vendorBillRef;delete l.purchaseBillRef;delete l.purchaseBill}})}else if(o.vendorId===id){o.vendorId='';delete o.vendorBillRef;delete o.purchaseBillRef;delete o.purchaseBill}})}
function deleteVendor(id){const db=read(),v=(db.vendors||[]).find(x=>x.id===id);if(!v)return;const use=vendorUsage(db,id);const extra=use.jobs||use.pays?`\n\nLinked jobs: ${use.jobs}\nVendor payments: ${use.pays}\n\nVendor master aur uski vendor-payment history delete hogi. Purane order ki production cost safe rahegi, sirf vendor link remove hoga.`:'';if(!confirm(`Delete vendor "${v.name||id}" permanently?${extra}`))return;db.vendors=(db.vendors||[]).filter(x=>x.id!==id);db.vendorPayments=(db.vendorPayments||[]).filter(p=>p.vendorId!==id);unlinkVendorFromOrders(db,id);remember('vendors');sessionStorage.setItem(MSG_KEY,`Vendor ${v.name||id} deleted`);write(db);setTimeout(()=>location.reload(),450)}
function patchVendorButtons(){$$('#vendorsBody tr').forEach(tr=>{const edit=tr.querySelector('[data-a="editVendor"]');if(!edit)return;const id=edit.dataset.id,box=edit.closest('.row-actions')||edit.parentElement;if(!box||box.querySelector('[data-v11="deleteVendor"]'))return;const b=document.createElement('button');b.type='button';b.className='icon-btn red';b.dataset.v11='deleteVendor';b.dataset.id=id;b.textContent='Delete';box.appendChild(b)})}

function orderLines(o){return Array.isArray(o?.lineItems)&&o.lineItems.length?o.lineItems:[o||{}]}
function flexLike(x){return ['Flex Printing','Wallpaper + Fitting'].includes(x?.service)}
function lineArea(x){return N(x.widthFt)&&N(x.heightFt)?N(x.widthFt)*N(x.heightFt)*Math.max(1,N(x.panels)||1):N(x.qty)}
function lineSale(x){const s=x?.service||'Other';if(flexLike(x))return lineArea(x)*N(x.rate)+N(x.designCharges)+N(x.fittingCharges)+N(x.otherCharges);if(s==='Passport Photos')return N(x.qty)*N(x.rate)+N(x.retouchCharges)+N(x.otherCharges);if(s==='ID / Document Print')return N(x.qty)*N(x.rate)+N(x.designCharges)+N(x.laminationCharges)+N(x.otherCharges);return N(x.qty)*N(x.rate)+N(x.designCharges)+N(x.finishingCharges)+N(x.fittingCharges)+N(x.otherCharges)}
function lineVendorCost(x){if(x?.service==='Offset / Packaging')return N(x.printingCost)+N(x.plateQty)*N(x.plateRate)+N(x.dieCost)+N(x.laminationCost)+N(x.uvFoilCost)+N(x.cuttingCost)+N(x.bindingCost)+N(x.packingCost)+N(x.transportCost)+N(x.vendorCost);return N(x?.vendorCost)}
function orderSale(o){return orderLines(o).reduce((a,l)=>a+lineSale(l),0)}
function orderVendorCost(o){return orderLines(o).reduce((a,l)=>a+lineVendorCost(l),0)}
function orderReceived(db,o){return N(o.advance)+(db.payments||[]).filter(p=>p.orderId===o.id).reduce((a,p)=>a+N(p.amount),0)}
function v16Card(label,value,foot,accent){return `<div class="stat-card" style="--accent:${accent}"><div class="stat-top"><span class="stat-label">${E(label)}</span></div><strong class="stat-value">${R(value)}</strong><div class="stat-foot">${E(foot)}</div></div>`}
function dashboardCorrect(box){const labels=[...box.querySelectorAll('.stat-label')].map(x=>x.textContent.trim());return box.querySelectorAll('.stat-card').length===6&&labels.join('|')==='Total Sales|Received|Pending Bills|Vendor Payment / Cost|Expenses|Net Profit'}
let dashSig='';
function patchDashboard(){const box=$('#dashboardCards');if(!box)return;const db=read(),orders=(db.orders||[]).filter(o=>o.status!=='Cancelled'),sales=orders.reduce((a,o)=>a+orderSale(o),0),received=orders.reduce((a,o)=>a+orderReceived(db,o),0),pending=orders.reduce((a,o)=>a+Math.max(0,orderSale(o)-orderReceived(db,o)),0),vendorCost=orders.reduce((a,o)=>a+orderVendorCost(o),0),vendorPaid=(db.vendorPayments||[]).reduce((a,p)=>a+N(p.amount),0),vendorPayable=Math.max(0,vendorCost-vendorPaid),expenses=(db.expenses||[]).reduce((a,e)=>a+N(e.amount),0),net=sales-vendorCost-expenses,sig=JSON.stringify([sales,received,pending,vendorCost,vendorPaid,vendorPayable,expenses,net,orders.length]);
 const brand=$('.brand span');if(brand&&brand.textContent!=='Agency ERP • GitHub + Cloud • V16')brand.textContent='Agency ERP • GitHub + Cloud • V16';
 if(sig===dashSig&&dashboardCorrect(box))return;dashSig=sig;box.dataset.v16='1';box.classList.add('dashboard-v16');box.innerHTML=v16Card('Total Sales',sales,`${orders.length} active orders`,'#3077d5')+v16Card('Received',received,'Customer collections received','#14945c')+v16Card('Pending Bills',pending,`${orders.filter(o=>Math.max(0,orderSale(o)-orderReceived(db,o))>0).length} customer bills pending`,'#e31b23')+v16Card('Vendor Payment / Cost',vendorCost,`Paid ${R(vendorPaid)} • Payable ${R(vendorPayable)}`,'#d98324')+v16Card('Expenses',expenses,'Office, material, fuel, salary, utilities etc.','#b95d2a')+v16Card('Net Profit',net,'Sales − Vendor Cost − Expenses','#14945c')}
function injectV16Style(){if($('#v16DashStyle'))return;const s=document.createElement('style');s.id='v16DashStyle';s.textContent='#dashboardCards.dashboard-v16{grid-template-columns:repeat(3,minmax(0,1fr))!important}@media(max-width:1000px){#dashboardCards.dashboard-v16{grid-template-columns:repeat(2,minmax(0,1fr))!important}}@media(max-width:600px){#dashboardCards.dashboard-v16{grid-template-columns:1fr!important}}';document.head.appendChild(s)}

function click(e){const nav=e.target.closest?.('.nav-btn[data-view],[data-jump]');if(nav){const v=nav.dataset.view||nav.dataset.jump;remember(v)}const b=e.target.closest?.('[data-v11="deleteVendor"]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();deleteVendor(b.dataset.id)}
let patchTimer=null;function schedulePatch(){clearTimeout(patchTimer);patchTimer=setTimeout(()=>{patchVendorButtons();patchDashboard();const v=activeView();if(validView(v))remember(v)},40)}
function init(){installSaveGuard();injectV16Style();document.addEventListener('click',click,true);restore();patchVendorButtons();patchDashboard();const msg=sessionStorage.getItem(MSG_KEY);if(msg){sessionStorage.removeItem(MSG_KEY);setTimeout(()=>toast(msg),150)}new MutationObserver(schedulePatch).observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});setTimeout(()=>{restore();patchDashboard()},120);setTimeout(()=>{restore();patchDashboard()},500);setTimeout(patchDashboard,1200)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();